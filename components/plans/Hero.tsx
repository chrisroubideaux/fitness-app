// components/plans/Hero.tsx
'use client';

//import Image from 'next/image';

export default function Hero() {
  return (
    <section
      className="plans-hero"
      style={{
        minHeight: '100vh', 
        overflow: 'hidden',
      }}
    >
      
      <div
        className="position-absolute top-0 start-0 w-100 h-100"
       
      />
    </section>
  );
}



{/*
'use client';

//import Image from 'next/image';

export default function Hero() {
  return (
    <section
      className="plans-hero"
      style={{
        minHeight: '100vh', 
        overflow: 'hidden',
      }}
    >
      
      <div
        className="position-absolute top-0 start-0 w-100 h-100"
        style={{
          background:
            'radial-gradient(circle at 70% 20%, rgba(128,0,255,0.15), transparent 60%)',
            height: '100%',
          zIndex: 1,
        }}
      />
    </section>
  );
}

*/}
